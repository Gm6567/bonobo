<!DOCTYPE html>


body {
    padding-top: 200px;
}

<html>
    <head>
        <meta charset="UTF-8" />
        <title>{% block title %}Bonobo network{% endblock %}</title>
        {% block stylesheets %} 

    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
     <link href="{{ asset('css/main.css') }}" type="text/css" rel="stylesheet" />

 <nav class="navbar navbar-inverse navbar-fixed-top">
      <div class="container">
        <div class="navbar-header">
          <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <a class="navbar-brand" href="#">Bonobo network</a>
        </div>
        <div id="navbar" class="collapse navbar-collapse">
          <ul class="nav navbar-nav">
            <li class="active"><a href="/">Page d'accueil</a></li>
            <li><a href="/login">Se connecter</a></li>
            <li><a href="/register">Créer un compte</a></li>
            <li><a href="/profile">Afficher mon profil</a></li>
            <li><a href="#">Liste des bonobos</a></li>
          </ul>
        </div><!--/.nav-collapse -->
      </div>
    </nav>
        <link rel="icon" type="image/x-icon" href="{{ asset('favicon.ico') }}" />
<div> 
<br />
<br />

            {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
                {{ 'Vous êtes connecté'|trans({'%username%': app.user.username}, 'FOSUserBundle') }} |
                <a href="{{ path('fos_user_security_logout') }}">
                    {{ 'Déconnexion'|trans({}, 'FOSUserBundle') }}
                </a>
           
            {% endif %}
        </div>

{% endblock %}
    </head>
    <body>
        {% block body %}{% endblock %}
        {% block javascripts %}{% endblock %}
    </body>
</html>
